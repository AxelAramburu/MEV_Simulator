/// <reference types="node" />
import { PublicKey } from "@solana/web3.js";
export declare type Address = PublicKey | string;
/**
 * @category Util
 */
export type PDA = {
    publicKey: PublicKey;
    bump: number;
};
/**
 * @category Util
 */
export declare class AddressUtil {
    static toPubKey(address: Address): PublicKey;
    static toPubKeys(addresses: Address[]): PublicKey[];
    static toString(address: Address): string;
    static toStrings(addresses: Address[]): string[];
    static findProgramAddress(seeds: (Uint8Array | Buffer)[], programId: PublicKey): PDA;
}
